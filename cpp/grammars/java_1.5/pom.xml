<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.javacc.codegen.cpp</groupId>
		<version>8.0.0-SNAPSHOT</version>
		<artifactId>grammars</artifactId>
	</parent>
	<groupId>org.javacc.codegen.cpp.grammars</groupId>
	<artifactId>java_1.5</artifactId>
	<packaging>nar</packaging>
	<name>JavaCC grammars C++  C</name>
	<build>
		<plugins>
			<plugin>
				<groupId>org.javacc.plugin</groupId>
				<artifactId>javacc-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>jcc</id>
						<phase>generate-sources</phase>
						<goals>
							<goal>javacc</goal>
						</goals>
						<configuration>
							<outputDirectory>target/generated-sources/cpp</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.github.maven-nar</groupId>
				<artifactId>nar-maven-plugin</artifactId>
				<extensions>true</extensions>
				<configuration>
					<libraries>
						<library>
							<type>executable</type>
							<run>true</run>
							<args>
								<arg>${project.basedir}/java/JavaGenerics.java</arg>
								<arg>${project.basedir}/java/JavaGenerics.out</arg>
								<arg>${project.basedir}/java/JavaGenerics.err</arg>
							</args>
						</library>
					</libraries>
					<cpp>
						<includePaths>
							<includePath>
								<path>${project.basedir}/src/main/cpp</path>
							</includePath>
							<includePath>
								<path>${project.basedir}/target/generated-sources/cpp</path>
							</includePath>
						</includePaths>
					</cpp>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>compare-out</id>
						<phase>test</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target unless="maven.test.skip">
								<available file="${project.basedir}/java/JavaGenerics.out" property="isActualAvailable" />
								<fail unless="isActualAvailable" message="${project.basedir}/java/JavaGenerics.out does not exist!" />
								<available file="${project.basedir}/java/JavaGenerics.ref" property="isExpectedAvailable" />
								<fail unless="isExpectedAvailable" message="${project.basedir}/java/JavaGenerics.ref does not exist!" />
								<condition property="files.match">
									<filesmatch file1="${project.basedir}/java/JavaGenerics.out" file2="${project.basedir}/java/JavaGenerics.ref" />
								</condition>
								<fail unless="files.match" message="${project.basedir}/java/JavaGenerics.out does not match ${project.basedir}/java/JavaGenerics.ref!" />
							</target>
						</configuration>
					</execution>
				</executions>
			</plugin>
 		</plugins>
	</build>
</project>